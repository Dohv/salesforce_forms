import React from 'react';

const Blue = () => {
  const styles = {
    backgroundColor: 'blue',
    color: 'white',
    height: '300px',
    width: '100vw'
  }
    return (

        <div style={styles}>
          Step 2
        </div>
    );
};

export default Blue;
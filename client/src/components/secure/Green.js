import React from 'react';

const Green = () => {
  const styles = {
    backgroundColor: 'green',
    color: 'white',
    height: '300px',
    width: '100vw'
  }
    return (

        <div style={styles}>
          Step 1
        </div>
    );
};

export default Green;
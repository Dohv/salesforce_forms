import React from 'react';

const Red = () => {
  const styles = {
    backgroundColor: 'red',
    color: 'white',
    height: '300px',
    width: '100vw'
  }
    return (

        <div style={styles}>
          Step 3
        </div>
    );
};

export default Red;